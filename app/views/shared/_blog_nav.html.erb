<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
  <div class="container">
    <a class="navbar-brand" href="index.html">Paulo Borba</a>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      Explore
      <i class="fa fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ml-auto">
        <%= nav_helper tag_type: 'li', tag_style: 'nav-item', link_style: 'text-white' %>
        <li class="nav-item">
          <%= login_link "text-white" if current_user.is_a?(GuestUser)  %>
        </li>
        <li class="nav-item">
          <%= register_link "text-white" if current_user.is_a?(GuestUser) %>
        </li>
        <li class="nav-item">
          <%= logout_link "text-white" unless current_user.is_a?(GuestUser) %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<header class="masthead" style="background-image: url(<%= img_asset_path local_assigns[:img_bg] %>)">
  <div class="overlay"></div>
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-md-10 mx-auto">
        <div class="<%= local_assigns[:head] %>">
          <h1><%= local_assigns[:title] %></h1>
          <%= show_subtitle local_assigns[:subtitle] %>
          <%= show_published_time local_assigns[:created_at] %>
        </div>
      </div>
    </div>
  </div>
</header>


